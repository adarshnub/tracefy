{
  "name": "@tracefy/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome runtime signal capture for Tracefy.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "typescript": "^5.9.0"
  }
}
